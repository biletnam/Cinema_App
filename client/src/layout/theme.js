import { colors } from './colors';

export const theme = {
  colors,
  font: {
    thin: 300,
    regular: 400,
    bold: 800,
  },
  media: {
    phone: '@media(max-width: 530px)',
    phonePlus: '@media(max-width: 660px)',
    tablet: '@media(max-width: 825px)',
    desktop: '@media(max-width: 1124px)',
  },
};
